# Install:
  - pygame
  - numpy

On a fresh installation, please run install_help.py before any attempts at using the login system, because it stops pickle from attempting to read a file that doesn't exist. I could code something to check for it, but that isn't worth my time at this point.
